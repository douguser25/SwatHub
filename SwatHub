-- ██████████████████████████████████████████
-- █         SWAT HUB V5                   █
-- █      by douguser25                    █
-- ██████████████████████████████████████████
-- Compatível: Delta, Fluxo, Synapse, Arceus X e outros

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local Humanoid = Character:WaitForChild("Humanoid")
local HRP = Character:WaitForChild("HumanoidRootPart")

pcall(function()
    local mt = getrawmetatable and getrawmetatable(game)
    if mt then
        local old = mt.__newindex
        setreadonly(mt, false)
        mt.__newindex = function(t,k,v)
            if k=="Parent" and tostring(v)=="nil" and t==LocalPlayer then return end
            return old(t,k,v) end
        setreadonly(mt, true) end end)

pcall(function()
    if syn and syn.protect_gui then syn.protect_gui(script) end
    if protect_gui then protect_gui(script) end end)

LocalPlayer.CharacterAdded:Connect(function(c)
    Character=c Humanoid=c:WaitForChild("Humanoid") HRP=c:WaitForChild("HumanoidRootPart") end)

local S = {
    Speed=16, Jump=50, Gravity=196, InfJump=false, Noclip=false, VoidProt=true,
    AntiFling=true, AntiSit=false, AntiFlash=true, AntiLag=true,
    RGBSpeed=3, RGBCorpo=false, RGBCabelo=false, RGBCasa=false, RGBCarro=false, RGBBike=false, RGBNomeBio=false,
    FlingOn=false, SpamOn=false, SpamMethod="Pistol", PropFling=false, PropBring=false, PropKill=false,
    LoopFogo=false, EfeitoVaiVem=false, EfeitoPadrao=false, VelocidadeCarro=200,
}

local rgbHue = 0
local tabButtons = {}

local Musicas = {
    Forro={"aquela sister","kaiky robsao","tava aqui pensando","boate antiga","beat piseiro","chuva de arroz","ela so quer me amar","A Culpa E Nossa","zum zum eu to voando"},
    Aleatoria={"Dias De Gloria","lil jao","prank","BROCASITO","oine","jogo da vida","carrolindo","video game","hotelcaro","coisa boa"},
    SoundVibes={"ap","crack GHB","fumo todos os baseados","apelido","bonde do discord","Sunflower funk","Perna bamba","Luz do Luar","fica comig ent","Meia Noite Remake"},
    Rock={"Keep Moving","From The Darkness","The Zero Line Encore"},
    Ingles={"beat love","shine","AllWorld","AllSugar","Armed","Lime","We Can Go Up Up Up","Miss The Rage","David Shawty","MONSTERS"},
    Rap={"Triste c","vixe","luar","SERRA","e dj moleque","SABADO","MANDRAKE","agora mudei","CONSIDERACAO"},
    Funk={"NUMBER","BRADAR","low Cortisol","da igreja","haru no fun","namorada nao me ama mais","SUBWAY SURFES","masetala trend","Bass Boost"},
    Phonk={"relaxed","morning Lo-fi","Japanese Lofi for Coffee","Ambient Soundscape","Space Voyage","Mesmeric","Atmospheric Dive","Understand","Morning","Rising Pattern"},
}

local SG=Instance.new("ScreenGui")
SG.Name="SwatHubV5" SG.ResetOnSpawn=false SG.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
pcall(function() if syn and syn.protect_gui then syn.protect_gui(SG) end end)
SG.Parent=gethui and gethui() or game.CoreGui

local TB=Instance.new("TextButton")
TB.Size=UDim2.new(0,55,0,55) TB.Position=UDim2.new(0,10,0.5,-27)
TB.BackgroundColor3=Color3.fromRGB(10,10,10) TB.BorderSizePixel=0
TB.Text="SWAT" TB.TextColor3=Color3.fromRGB(255,255,255)
TB.TextScaled=true TB.Font=Enum.Font.GothamBold TB.ZIndex=100 TB.Parent=SG
Instance.new("UICorner",TB).CornerRadius=UDim.new(1,0)
local tbs=Instance.new("UIStroke",TB) tbs.Color=Color3.fromRGB(255,255,255) tbs.Thickness=2

local dT,dTs,dTp=false,nil,nil
TB.InputBegan:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseButton1 then
        dT=true dTs=i.Position dTp=TB.Position end end)

local MF=Instance.new("Frame")
MF.Size=UDim2.new(0,560,0,430) MF.Position=UDim2.new(0.5,-280,0.5,-215)
MF.BackgroundColor3=Color3.fromRGB(8,8,8) MF.BorderSizePixel=0 MF.Visible=false MF.ZIndex=10 MF.Parent=SG
Instance.new("UICorner",MF).CornerRadius=UDim.new(0,10)
local mfs=Instance.new("UIStroke",MF) mfs.Color=Color3.fromRGB(200,200,200) mfs.Thickness=1.5

local Top=Instance.new("Frame",MF)
Top.Size=UDim2.new(1,0,0,45) Top.BackgroundColor3=Color3.fromRGB(15,15,15) Top.BorderSizePixel=0 Top.ZIndex=11
Instance.new("UICorner",Top).CornerRadius=UDim.new(0,10)

local Title=Instance.new("TextLabel",Top)
Title.Size=UDim2.new(1,-50,1,0) Title.Position=UDim2.new(0,15,0,0)
Title.BackgroundTransparency=1 Title.Text="SWAT HUB  V5   by douguser25"
Title.TextColor3=Color3.fromRGB(255,255,255) Title.TextScaled=true
Title.Font=Enum.Font.GothamBold Title.TextXAlignment=Enum.TextXAlignment.Left Title.ZIndex=12

local XBtn=Instance.new("TextButton",Top)
XBtn.Size=UDim2.new(0,35,0,35) XBtn.Position=UDim2.new(1,-40,0,5)
XBtn.BackgroundColor3=Color3.fromRGB(30,30,30) XBtn.Text="✕" XBtn.TextColor3=Color3.fromRGB(255,255,255)
XBtn.TextScaled=true XBtn.Font=Enum.Font.GothamBold XBtn.BorderSizePixel=0 XBtn.ZIndex=13
Instance.new("UICorner",XBtn).CornerRadius=UDim.new(1,0)
XBtn.MouseButton1Click:Connect(function() MF.Visible=false end)

local dM,dMs,dMp=false,nil,nil
Top.InputBegan:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseButton1 then
        dM=true dMs=i.Position dMp=MF.Position end end)

UserInputService.InputChanged:Connect(function(i)
    if dM and (i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseMovement) then
        local d=i.Position-dMs
        MF.Position=UDim2.new(dMp.X.Scale,dMp.X.Offset+d.X,dMp.Y.Scale,dMp.Y.Offset+d.Y) end
    if dT and (i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseMovement) then
        local d=i.Position-dTs
        TB.Position=UDim2.new(dTp.X.Scale,dTp.X.Offset+d.X,dTp.Y.Scale,dTp.Y.Offset+d.Y) end end)

UserInputService.InputEnded:Connect(function(i)
    if i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseButton1 then
        dM=false dT=false end end)

TB.MouseButton1Click:Connect(function() MF.Visible=not MF.Visible end)

local SB=Instance.new("ScrollingFrame",MF)
SB.Size=UDim2.new(0,150,1,-45) SB.Position=UDim2.new(0,0,0,45)
SB.BackgroundColor3=Color3.fromRGB(12,12,12) SB.BorderSizePixel=0
SB.ScrollBarThickness=0 SB.CanvasSize=UDim2.new(0,0,0,0) SB.AutomaticCanvasSize=Enum.AutomaticSize.Y SB.ZIndex=11
local SBL=Instance.new("UIListLayout",SB) SBL.SortOrder=Enum.SortOrder.LayoutOrder SBL.Padding=UDim.new(0,2)

local Div=Instance.new("Frame",MF)
Div.Size=UDim2.new(0,1,1,-45) Div.Position=UDim2.new(0,151,0,45)
Div.BackgroundColor3=Color3.fromRGB(50,50,50) Div.BorderSizePixel=0 Div.ZIndex=11

local CA=Instance.new("ScrollingFrame",MF)
CA.Size=UDim2.new(1,-158,1,-50) CA.Position=UDim2.new(0,155,0,48)
CA.BackgroundTransparency=1 CA.BorderSizePixel=0
CA.ScrollBarThickness=3 CA.ScrollBarImageColor3=Color3.fromRGB(180,180,180)
CA.CanvasSize=UDim2.new(0,0,0,0) CA.AutomaticCanvasSize=Enum.AutomaticSize.Y CA.ZIndex=11
local CAL=Instance.new("UIListLayout",CA) CAL.SortOrder=Enum.SortOrder.LayoutOrder CAL.Padding=UDim.new(0,5)
local CAP=Instance.new("UIPadding",CA)
CAP.PaddingLeft=UDim.new(0,8) CAP.PaddingRight=UDim.new(0,8) CAP.PaddingTop=UDim.new(0,8)

local function Clear()
    for _,v in pairs(CA:GetChildren()) do
        if not v:IsA("UIListLayout") and not v:IsA("UIPadding") then v:Destroy() end end end

local function Sec(t)
    local l=Instance.new("TextLabel",CA)
    l.Size=UDim2.new(1,0,0,28) l.BackgroundTransparency=1 l.Text=t
    l.TextColor3=Color3.fromRGB(255,255,255) l.Font=Enum.Font.GothamBold
    l.TextSize=14 l.TextXAlignment=Enum.TextXAlignment.Left l.ZIndex=12 end

local function Tog(lbl,desc,def,cb)
    local row=Instance.new("Frame",CA)
    row.Size=UDim2.new(1,0,0,52) row.BackgroundColor3=Color3.fromRGB(18,18,18) row.BorderSizePixel=0 row.ZIndex=12
    Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
    local l=Instance.new("TextLabel",row) l.Size=UDim2.new(0.65,0,0.5,0) l.Position=UDim2.new(0,10,0,2)
    l.BackgroundTransparency=1 l.Text=lbl l.TextColor3=Color3.fromRGB(220,220,220) l.Font=Enum.Font.Gotham l.TextSize=13 l.TextXAlignment=Enum.TextXAlignment.Left l.ZIndex=13
    local d=Instance.new("TextLabel",row) d.Size=UDim2.new(0.65,0,0.45,0) d.Position=UDim2.new(0,10,0.5,0)
    d.BackgroundTransparency=1 d.Text=desc d.TextColor3=Color3.fromRGB(120,120,120) d.Font=Enum.Font.Gotham d.TextSize=11 d.TextXAlignment=Enum.TextXAlignment.Left d.ZIndex=13
    local state=def or false
    local bg=Instance.new("Frame",row) bg.Size=UDim2.new(0,46,0,26) bg.Position=UDim2.new(1,-56,0.5,-13)
    bg.BackgroundColor3=state and Color3.fromRGB(255,255,255) or Color3.fromRGB(50,50,50) bg.BorderSizePixel=0 bg.ZIndex=13
    Instance.new("UICorner",bg).CornerRadius=UDim.new(1,0)
    local ball=Instance.new("Frame",bg) ball.Size=UDim2.new(0,20,0,20)
    ball.Position=state and UDim2.new(1,-23,0.5,-10) or UDim2.new(0,3,0.5,-10)
    ball.BackgroundColor3=Color3.fromRGB(10,10,10) ball.BorderSizePixel=0 ball.ZIndex=14
    Instance.new("UICorner",ball).CornerRadius=UDim.new(1,0)
    local ca=Instance.new("TextButton",row) ca.Size=UDim2.new(1,0,1,0) ca.BackgroundTransparency=1 ca.Text="" ca.ZIndex=15
    ca.MouseButton1Click:Connect(function()
        state=not state
        TweenService:Create(bg,TweenInfo.new(0.15),{BackgroundColor3=state and Color3.fromRGB(255,255,255) or Color3.fromRGB(50,50,50)}):Play()
        TweenService:Create(ball,TweenInfo.new(0.15),{Position=state and UDim2.new(1,-23,0.5,-10) or UDim2.new(0,3,0.5,-10)}):Play()
        if cb then cb(state) end end) end

local function Btn(lbl,desc,cb)
    local row=Instance.new("Frame",CA)
    row.Size=UDim2.new(1,0,0,52) row.BackgroundColor3=Color3.fromRGB(18,18,18) row.BorderSizePixel=0 row.ZIndex=12
    Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
    local l=Instance.new("TextLabel",row) l.Size=UDim2.new(0.8,0,0.5,0) l.Position=UDim2.new(0,10,0,2)
    l.BackgroundTransparency=1 l.Text=lbl l.TextColor3=Color3.fromRGB(220,220,220) l.Font=Enum.Font.Gotham l.TextSize=13 l.TextXAlignment=Enum.TextXAlignment.Left l.ZIndex=13
    local d=Instance.new("TextLabel",row) d.Size=UDim2.new(0.8,0,0.45,0) d.Position=UDim2.new(0,10,0.5,0)
    d.BackgroundTransparency=1 d.Text=desc d.TextColor3=Color3.fromRGB(120,120,120) d.Font=Enum.Font.Gotham d.TextSize=11 d.TextXAlignment=Enum.TextXAlignment.Left d.ZIndex=13
    local ar=Instance.new("TextLabel",row) ar.Size=UDim2.new(0,20,1,0) ar.Position=UDim2.new(1,-28,0,0)
    ar.BackgroundTransparency=1 ar.Text="›" ar.TextColor3=Color3.fromRGB(180,180,180) ar.Font=Enum.Font.GothamBold ar.TextSize=22 ar.ZIndex=13
    local ca=Instance.new("TextButton",row) ca.Size=UDim2.new(1,0,1,0) ca.BackgroundTransparency=1 ca.Text="" ca.ZIndex=15
    ca.MouseButton1Click:Connect(function() if cb then cb() end end) end

local function Sld(lbl,desc,mn,mx,def,cb)
    local row=Instance.new("Frame",CA)
    row.Size=UDim2.new(1,0,0,65) row.BackgroundColor3=Color3.fromRGB(18,18,18) row.BorderSizePixel=0 row.ZIndex=12
    Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
    local l=Instance.new("TextLabel",row) l.Size=UDim2.new(0.7,0,0,22) l.Position=UDim2.new(0,10,0,4)
    l.BackgroundTransparency=1 l.Text=lbl l.TextColor3=Color3.fromRGB(220,220,220) l.Font=Enum.Font.Gotham l.TextSize=13 l.TextXAlignment=Enum.TextXAlignment.Left l.ZIndex=13
    local vl=Instance.new("TextLabel",row) vl.Size=UDim2.new(0.25,0,0,22) vl.Position=UDim2.new(0.73,0,0,4)
    vl.BackgroundTransparency=1 vl.Text=tostring(def) vl.TextColor3=Color3.fromRGB(255,255,255) vl.Font=Enum.Font.GothamBold vl.TextSize=13 vl.TextXAlignment=Enum.TextXAlignment.Right vl.ZIndex=13
    local sbg=Instance.new("Frame",row) sbg.Size=UDim2.new(1,-20,0,8) sbg.Position=UDim2.new(0,10,0,42)
    sbg.BackgroundColor3=Color3.fromRGB(40,40,40) sbg.BorderSizePixel=0 sbg.ZIndex=13
    Instance.new("UICorner",sbg).CornerRadius=UDim.new(1,0)
    local sf=Instance.new("Frame",sbg) sf.Size=UDim2.new((def-mn)/(mx-mn),0,1,0)
    sf.BackgroundColor3=Color3.fromRGB(200,200,200) sf.BorderSizePixel=0 sf.ZIndex=14
    Instance.new("UICorner",sf).CornerRadius=UDim.new(1,0)
    local sl=false
    sbg.InputBegan:Connect(function(i) if i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseButton1 then sl=true end end)
    UserInputService.InputEnded:Connect(function(i) if i.UserInputType==Enum.UserInputType.Touch or i.UserInputType==Enum.UserInputType.MouseButton1 then sl=false end end)
    UserInputService.InputChanged:Connect(function(i)
        if sl then
            local p=math.clamp((i.Position.X-sbg.AbsolutePosition.X)/sbg.AbsoluteSize.X,0,1)
            local v=math.floor(mn+(mx-mn)*p)
            sf.Size=UDim2.new(p,0,1,0) vl.Text=tostring(v)
            if cb then cb(v) end end end) end

local function Tab(name,icon,order)
    local b=Instance.new("TextButton",SB)
    b.Size=UDim2.new(1,0,0,40) b.BackgroundColor3=Color3.fromRGB(15,15,15) b.BorderSizePixel=0
    b.Text=icon.."  "..name b.TextColor3=Color3.fromRGB(160,160,160) b.Font=Enum.Font.Gotham
    b.TextSize=13 b.TextXAlignment=Enum.TextXAlignment.Left b.ZIndex=12 b.LayoutOrder=order
    local p=Instance.new("UIPadding",b) p.PaddingLeft=UDim.new(0,12)
    tabButtons[name]=b return b end

local function SetTab(name)
    for n,b in pairs(tabButtons) do
        if n==name then b.BackgroundColor3=Color3.fromRGB(35,35,35) b.TextColor3=Color3.fromRGB(255,255,255)
        else b.BackgroundColor3=Color3.fromRGB(15,15,15) b.TextColor3=Color3.fromRGB(160,160,160) end end
    Clear() end

local c1=Tab("Credits","ℹ",1)
c1.MouseButton1Click:Connect(function()
    SetTab("Credits") Sec("Créditos do Hub")
    local f=Instance.new("Frame",CA) f.Size=UDim2.new(1,0,0,110) f.BackgroundColor3=Color3.fromRGB(18,18,18) f.BorderSizePixel=0 f.ZIndex=12
    Instance.new("UICorner",f).CornerRadius=UDim.new(0,7)
    local function AI(lbl,val,y) local l=Instance.new("TextLabel",f) l.Size=UDim2.new(1,-20,0,22) l.Position=UDim2.new(0,10,0,y)
        l.BackgroundTransparency=1 l.Text=lbl..":  "..val l.TextColor3=Color3.fromRGB(200,200,200) l.Font=Enum.Font.Gotham l.TextSize=13 l.TextXAlignment=Enum.TextXAlignment.Left l.ZIndex=13 end
    AI("Hub","SWAT HUB V5",6) AI("Versão","V5",30) AI("Criador","douguser25",54) AI("By","douguser25",78) end)

local c2=Tab("Jogador","👤",2)
c2.MouseButton1Click:Connect(function()
    SetTab("Jogador") Sec("Farm do Evento")
    Tog("Auto Farm 1","Pega ovos e tokens automaticamente",false,function(v) end)
    Tog("Auto Farm 2","Farma APENAS no Hard ou Extreme",false,function(v) end)
    Sec("jogador")
    Sld("Speed Player","Velocidade do jogador",1,100,S.Speed,function(v) S.Speed=v if Humanoid then Humanoid.WalkSpeed=v end end)
    Sld("JumpPower","Altura do pulo",1,200,S.Jump,function(v) S.Jump=v if Humanoid then Humanoid.JumpPower=v end end)
    Sld("Gravity","Gravidade",10,500,S.Gravity,function(v) S.Gravity=v workspace.Gravity=v end)
    Tog("Infinite Jump","Pular infinitamente",S.InfJump,function(v) S.InfJump=v end)
    Btn("Resetar Status (Speed/Jump/Gravity).","Reseta valores padrão",function()
        S.Speed=16 S.Jump=50 S.Gravity=196
        if Humanoid then Humanoid.WalkSpeed=16 Humanoid.JumpPower=50 end workspace.Gravity=196 end)
    Sec("para o fogo e preciso ter uma casa com churrasqueira")
    Btn("Ativar Fogo","Ativa o fogo",function() end)
    Tog("Loop Fogo","Ativa o fogo em loop",S.LoopFogo,function(v) S.LoopFogo=v end)
    Sec("Modificar Nome e Bio")
    Tog("Efeito de texto Vai Vem","Nome fica se movendo",S.EfeitoVaiVem,function(v) S.EfeitoVaiVem=v end)
    Tog("Efeito de texto padrão","Efeito padrão no nome",S.EfeitoPadrao,function(v) S.EfeitoPadrao=v end)
    Sec("EXTRA - Fontes Personalizadas")
    Btn("Aplicar Nome","Aplica nome personalizado",function() end)
    Btn("Aplicar Bio","Aplica bio personalizada",function() end) end)

local c3=Tab("Jogadores","👥",3)
c3.MouseButton1Click:Connect(function()
    SetTab("Jogadores") Sec("jogadores")
    Btn("Atualizar Lista de Jogadores","Atualiza a lista do servidor",function()
        for _,v in pairs(CA:GetChildren()) do if v.Name=="PLR" then v:Destroy() end end
        for _,p in pairs(Players:GetPlayers()) do
            if p~=LocalPlayer then
                local row=Instance.new("Frame",CA) row.Name="PLR" row.Size=UDim2.new(1,0,0,44)
                row.BackgroundColor3=Color3.fromRGB(18,18,18) row.BorderSizePixel=0 row.ZIndex=12
                Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)
                local nl=Instance.new("TextLabel",row) nl.Size=UDim2.new(0.55,0,1,0) nl.Position=UDim2.new(0,10,0,0)
                nl.BackgroundTransparency=1 nl.Text=p.Name nl.TextColor3=Color3.fromRGB(220,220,220)
                nl.Font=Enum.Font.GothamBold nl.TextSize=12 nl.TextXAlignment=Enum.TextXAlignment.Left nl.ZIndex=13
                local tb2=Instance.new("TextButton",row) tb2.Size=UDim2.new(0,65,0,26) tb2.Position=UDim2.new(1,-75,0.5,-13)
                tb2.BackgroundColor3=Color3.fromRGB(50,50,50) tb2.Text="Teleport" tb2.TextColor3=Color3.fromRGB(255,255,255)
                tb2.Font=Enum.Font.Gotham tb2.TextSize=11 tb2.BorderSizePixel=0 tb2.ZIndex=13
                Instance.new("UICorner",tb2).CornerRadius=UDim.new(0,6)
                tb2.MouseButton1Click:Connect(function()
                    if p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                        HRP.CFrame=p.Character.HumanoidRootPart.CFrame+Vector3.new(2,0,0) end end) end end end)
    Btn("Teleportar para Jogador","Teleporta para o jogador selecionado",function() end)
    Tog("Visualizar Jogador","Ativa ESP no jogador",false,function(v) end)
    Sec("Head Sit")
    Btn("Head Sit","Senta na cabeça do jogador selecionado",function() end)
    Sec("trocar nome das placas")
    Btn("Placa troca all","Troca placa de todos",function() end)
    Btn("Placa troca selecionado","Troca placa do selecionado",function() end)
    Sec("Quem vai ser molestado(a)?")
    Tog("Prop Molestador","Molesta com prop no chão",false,function(v) end)
    Sec("Funções Props")
    Tog("Girar Prop","Gira o prop",false,function(v) end)
    Tog("Prop RGB","RGB no prop",false,function(v) end)
    Sec("Configurações Prop")
    Sld("Subir e Descer Prop","Altura do prop",-10,10,-5,function(v) end)
    Sld("Distância do Jogador","Distância do prop",1,10,3,function(v) end)
    Sld("Inclinar para Frente e Trás","Inclinação",-10,10,0,function(v) end)
    Sld("Virar para Esquerda e Direita","Rotação",-10,10,0,function(v) end)
    Sld("Inclinar para os Lados","Inclinação lateral",-10,10,0,function(v) end)
    Sec("Boneco Seguidor HAHAHA Sus")
    Btn("faz o boneco seguir o jogador","Boneco segue o jogador",function() end)
    Btn("Retornar boneco","Retorna o boneco",function() end)
    Tog("Braço Sus jogador","Braço sus no jogador",false,function(v) end)
    Tog("Sus atras do jogador","Sus atrás do jogador",false,function(v) end)
    Tog("Sus cara do jogador","Sus na cara do jogador",false,function(v) end) end)

local c4=Tab("Extra","✨",4)
c4.MouseButton1Click:Connect(function()
    SetTab("Extra") Sec("Extras")
    Btn("God Mode","Personagem invencível",function() if Humanoid then Humanoid.MaxHealth=math.huge Humanoid.Health=math.huge end end)
    Btn("Okarum","Animação especial",function() end)
    Btn("Mago","Efeito de mago",function() end)
    Btn("Glitcher Hat","Chapéu glitch",function() end)
    Btn("Cowboy","Chapéu de cowboy",function() end)
    Btn("Superman","Pose de Superman",function() end) end)

local c5=Tab("Avatar","👕",5)
c5.MouseButton1Click:Connect(function()
    SetTab("Avatar") Sec("Copy Avatar")
    Btn("Atualizar lista","Atualiza jogadores",function() end)
    Btn("Copy Avatar","Copia avatar do jogador",function() end)
    Sec("Partes do corpo personalizado.")
    Btn("Equipar Tronco","Aplica ID no tronco",function() end)
    Btn("Equipar Braço Direito","Aplica ID no braço direito",function() end)
    Btn("Equipar Braço Esquerdo","Aplica ID no braço esquerdo",function() end)
    Btn("Equipar Perna Direita","Aplica ID na perna direita",function() end)
    Btn("Equipar Perna Esquerda","Aplica ID na perna esquerda",function() end)
    Btn("Equipar Cabeça","Aplica ID na cabeça",function() end)
    Sec("R6 Avatars")
    Btn("R6 FE","Avatar R6 FE",function() end)
    Btn("2009s R6","Avatar 2009 R6",function() end)
    Sec("Homi")
    Btn("menino magrelo (White)","Avatar menino magrelo",function() end)
    Btn("musculoso da perna magrela","Avatar musculoso",function() end)
    Btn("Mini homi","Avatar mini homi",function() end)
    Btn("gordinho","Avatar gordinho",function() end)
    Btn("grande cinza","Avatar grande cinza",function() end)
    Sec("Mini Plusie")
    Btn("Mini Plusie (Black)","Avatar mini plusie preto",function() end)
    Btn("Mini Plusie (White)","Avatar mini plusie branco",function() end)
    Btn("criança da moda","Avatar criança da moda",function() end)
    Btn("criança torta","Avatar criança torta",function() end) end)

local c6=Tab("House","🏠",6)
c6.MouseButton1Click:Connect(function()
    SetTab("House") Sec("funções de casa")
    Btn("Desbanir das Casas","Remove ban de todas as casas",function() end)
    Tog("Auto Unban Casas","Desbanir automaticamente",false,function(v) end)
    Btn("Atualizar Casas","Atualiza lista de casas",function() end)
    Btn("TP para Cofre","Teleporta para o cofre",function() end)
    Btn("teleporta para Casa","Vai para sua casa",function() end)
    Tog("Atravessar Porta da casa selecionanda","Passa por portas",false,function(v) end)
    Tog("Loop Tocar Campainha","Toca campainha em loop",false,function(v) end)
    Tog("Loop Bater na Porta","Bate na porta em loop",false,function(v) end) end)

local c7=Tab("Car","🚗",7)
c7.MouseButton1Click:Connect(function()
    SetTab("Car") Sec("Speed e Turbo (Meu Carro)")
    Sld("Velocidade Máxima","Velocidade do carro",50,500,200,function(v) S.VelocidadeCarro=v end)
    Btn("Aplicar Velocidade","Aplica velocidade no carro",function()
        local seat=LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("VehicleSeat")
        if seat then seat.MaxSpeed=S.VelocidadeCarro end end)
    Btn("Aplicar Turbo","Aplica turbo no carro",function() end)
    Sec("Interação com Veículos do Servidor")
    Btn("Destruir Carro Selecionado","Remove o carro",function() end)
    Btn("Trazer Carro Selecionado","Traz o carro",function() end)
    Tog("Ver Câmera do Carro Selecionado","Câmera do carro",false,function(v) end)
    Btn("Trazer Todos os Carros","Traz todos os carros",function() end)
    Tog("Matar Todos os Carros (Loop)","Joga carros no void",false,function(v) end) end)

local c8=Tab("Tools","🔧",8)
c8.MouseButton1Click:Connect(function()
    SetTab("Tools") Sec("Custom Word")
    Btn("Build Custom Word","Constrói palavra com Tools",function() end)
    Sec("Spam de Tools")
    for _,v in pairs({"Spam Pistol","Spam Sniper","Spam Bomb","Spam Celular","Spam Extintor"}) do
        Btn(v,"Selecionar método: "..v,function() S.SpamMethod=v end) end
    Tog("Ativar Spam","Ativa spam (use noclip+antilag)",S.SpamOn,function(v) S.SpamOn=v end) end)

local c9=Tab("RGB","🎨",9)
c9.MouseButton1Click:Connect(function()
    SetTab("RGB")
    Sld("Velocidade RGB","Velocidade do efeito RGB",1,10,S.RGBSpeed,function(v) S.RGBSpeed=v end)
    Sec("RGB para usar em você")
    Tog("Nome + Bio RGB","Nome e bio mudam de cor",S.RGBNomeBio,function(v) S.RGBNomeBio=v end)
    Tog("RGB Corpo","Efeito RGB no corpo",S.RGBCorpo,function(v) S.RGBCorpo=v end)
    Tog("RGB Cabelo","Efeito RGB no cabelo",S.RGBCabelo,function(v) S.RGBCabelo=v end)
    Sec("RGB para casa e veículos")
    Tog("RGB Casa","Efeito RGB na casa",S.RGBCasa,function(v) S.RGBCasa=v end)
    Tog("Carro RGB (Premium)","Altera cor do carro",S.RGBCarro,function(v) S.RGBCarro=v end)
    Tog("RGB Bicicleta","Efeito RGB na bicicleta",S.RGBBike,function(v) S.RGBBike=v end)
    Sec("RGB para usar em objetos") end)

local c10=Tab("Music All","🎵",10)
c10.MouseButton1Click:Connect(function()
    SetTab("Music All") Sec("Tocar Som no Servidor apenas 3 segundos")
    Btn("Tocar Som","Toca o áudio no servidor",function() end)
    Tog("Loop","Coloca o som em loop",false,function(v) end)
    Sec("tocar meme em todo o server")
    Btn("Pankapakan","Toca som pankapakan",function() end)
    Btn("Meme Inglês","Toca meme em inglês",function() end)
    Btn("Risada","Toca som de risada",function() end)
    Btn("Gemido","Toca som de gemido",function() end)
    Btn("Terror ou Efeito (Jumpscar)","Toca terror/efeito",function() end)
    Tog("Ouvir meu próprio áudio","Ouvir o áudio também em você",false,function(v) end)
    Btn("Tocar Som Selecionado","Toca o som do dropdown",function() end)
    Sld("Velocidade do Loop","Menor = mais rápido",1,10,1,function(v) end) end)

local c11=Tab("Music","🎶",11)
c11.MouseButton1Click:Connect(function()
    SetTab("Music") Sec("Músicas")
    local cats={
        {"Forró",Musicas.Forro},{"Músicas e Memes Aleatório",Musicas.Aleatoria},
        {"SoundVibes",Musicas.SoundVibes},{"Rock",Musicas.Rock},
        {"ingles",Musicas.Ingles},{"rép",Musicas.Rap},
        {"Funk",Musicas.Funk},{"Phonk",Musicas.Phonk},}
    for _,cat in pairs(cats) do
        Sec(cat[1])
        for _,m in pairs(cat[2]) do Btn(m,"Tocar: "..m,function() end) end end
    Btn("Stop ALL music","Para todas as músicas",function()
        for _,v in pairs(workspace:GetDescendants()) do if v:IsA("Sound") then v:Stop() end end end) end)

local c12=Tab("Troll","😈",12)
c12.MouseButton1Click:Connect(function()
    SetTab("Troll") Sec("Selecionar Jogador")
    Btn("Atualizar lista","Atualiza jogadores para troll",function() end)
    Btn("Teleportar para Jogador","Teleporta para o jogador",function() end)
    Tog("Visualizar Jogador","Ver o jogador selecionado",false,function(v) end)
    Sec("Kill")
    Btn("Matar","Inicia o matar com método selecionado",function() end)
    Btn("Puxar","Inicia o puxar so com sofa",function() end)
    Btn("Parar (Matar ou Puxar)","Para o movimento de matar ou puxar",function() end)
    Sec("Prop")
    Tog("Prop Fling","Fling com prop",S.PropFling,function(v) S.PropFling=v end)
    Tog("Prop Bring","Bring com prop",S.PropBring,function(v) S.PropBring=v end)
    Tog("Prop Kill","Kill com prop",S.PropKill,function(v) S.PropKill=v end)
    Btn("House Ban Kill","Bane e mata jogador da casa",function() end)
    Sec("jail players")
    Btn("Prender Jogador","Coloca jogador na prisão",function() end)
    Sec("Fling")
    Tog("Ativar Fling","Lança jogadores (use noclip)",S.FlingOn,function(v) S.FlingOn=v end)
    Btn("Bola Fling Orbitando","Fling com bola orbitando",function() end)
    Btn("Fling Bola ALL V4","Fling em todos",function() end)
    Btn("Parar Tudo","Para todos os trolls",function() S.FlingOn=false S.SpamOn=false S.PropFling=false end)
    Sec("Bring All")
    Btn("iniciar bring all Bus","Traz todos pro bus",function() end)
    Btn("parar bring all Bus","Para bring all bus",function() end)
    Btn("iniciar Bring All couch","Traz todos pro couch",function() end)
    Btn("parar Bring All couch","Para bring all couch",function() end) end)

local c13=Tab("Protection","🛡",13)
c13.MouseButton1Click:Connect(function()
    SetTab("Protection")
    Tog("Anti-Sit","Impede o jogador de sentar",S.AntiSit,function(v) S.AntiSit=v end)
    Sec("proteções")
    Tog("Noclip","Atravessa paredes e objetos",S.Noclip,function(v) S.Noclip=v end)
    Tog("Void Protection","Protege contra quedas no vazio",S.VoidProt,function(v) S.VoidProt=v end)
    Tog("Anti-Fling","Anfling contra outros scripters",S.AntiFling,function(v) S.AntiFling=v end)
    Tog("Anti Flashbang / skybox","Deixa vc imune a essa polinha",S.AntiFlash,function(v) S.AntiFlash=v end)
    Tog("Anti-Lag","Anti-lag para tools e objetos no player",S.AntiLag,function(v) S.AntiLag=v end) end)

SetTab("Credits")
c1.MouseButton1Click:Fire()

UserInputService.JumpRequest:Connect(function()
    if S.InfJump and Humanoid then Humanoid:ChangeState(Enum.HumanoidStateType.Jumping) end end)

RunService.Stepped:Connect(function()
    if S.Noclip and Character then
        for _,p in pairs(Character:GetDescendants()) do
            if p:IsA("BasePart") then p.CanCollide=false end end end end)

RunService.Heartbeat:Connect(function()
    if S.VoidProt and HRP then if HRP.Position.Y<-80 then HRP.CFrame=CFrame.new(0,10,0) end end
    if S.AntiFling and HRP then if HRP.Velocity.Magnitude>200 then HRP.Velocity=Vector3.new(0,0,0) end end end)

RunService.Heartbeat:Connect(function(dt)
    rgbHue=(rgbHue+dt*S.RGBSpeed*0.05)%1
    local color=Color3.fromHSV(rgbHue,1,1)
    if S.RGBCorpo and Character then
        for _,p in pairs(Character:GetDescendants()) do
            if p:IsA("BasePart") and p.Name~="HumanoidRootPart" then p.Color=color end end end end)

print("✅ SWAT HUB V5 carregado! by douguser25")
